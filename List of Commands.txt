heroku login

pip install gunicorn
pip freeze requirements.txt
Procfile
web:gunicorn app:app

from heroku dasboard select buildpack - the project language
git init
git add .
git commit -m "Initial commmit"

heroku create todo-veeren
git remote -v
git push heroku master
